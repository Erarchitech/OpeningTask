<Window x:Class="OpeningTask.Views.DuplicateCuboidsReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Предупреждение" Height="420" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="#FAFAFA">

	<Grid Margin="12">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<TextBlock Grid.Row="0"
                   TextWrapping="Wrap"
                   FontWeight="Bold"
                   Margin="0,0,0,8"
                   Text="Обнаружены дубликаты боксов в точках размещения. Существующие экземпляры перечислены ниже. Новые экземпляры для этих точек не были созданы." />

		<Expander Grid.Row="1" Header="ID существующих боксов" IsExpanded="false" Margin="0,0,0,8">
			<Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1" Padding="6">
				<ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" MaxHeight="220">
					<ItemsControl x:Name="IdsItemsControl">
						<ItemsControl.ItemTemplate>
							<DataTemplate>
								<TextBlock Text="{Binding}"/>
							</DataTemplate>
						</ItemsControl.ItemTemplate>
					</ItemsControl>
				</ScrollViewer>
			</Border>
		</Expander>

		<TextBox Grid.Row="2" x:Name="ReportTextBox"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Background="White"
                 BorderBrush="#DDDDDD"
                 BorderThickness="1"/>

		<DockPanel Grid.Row="3" Margin="0,10,0,0" LastChildFill="False">
			<Button x:Name="SaveButton"
                    Content="Сохранить отчёт в .txt"
                    Width="170"
                    Height="30"
                    Margin="0,0,10,0"
                    DockPanel.Dock="Left"/>

			<Button x:Name="OkButton"
                    Content="ОК"
                    Width="90"
                    Height="30"
                    DockPanel.Dock="Right"
                    IsDefault="True"/>
		</DockPanel>
	</Grid>
</Window>